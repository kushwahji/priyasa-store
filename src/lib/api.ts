const BASE=process.env.PRIYASA_API_URL||'http://localhost:8000/api/v1';
export async function api<T>(path:string,init:RequestInit={}){const headers=new Headers(init.headers);headers.set('Accept','application/json');if(init.body&&!headers.has('Content-Type'))headers.set('Content-Type','application/json');const token=typeof window!=='undefined'?localStorage.getItem('priyasa_access_token'):null;if(token)headers.set('Authorization',`Bearer ${token}`);const res=await fetch(`${BASE}${path}`,{...init,headers,cache:'no-store'});if(res.status===401&&typeof window!=='undefined')localStorage.removeItem('priyasa_access_token');if(!res.ok)throw new Error(`PRIYASA_API_${res.status}`);return res.json() as Promise<T>}
