'use client';
import {useState} from 'react';
export default function Login(){const [phone,setPhone]=useState('');const [sent,setSent]=useState(false);return <main className="container"><section className="section" style={{maxWidth:520,margin:'60px auto'}}><span className="eyebrow">PRIYASA ACCOUNT</span><h1>{sent?'Verify your OTP':'Sign in'}</h1><p className="muted">{sent?`OTP sent to ${phone}. Connect this form to the PriyasaCore OTP endpoint.`:'Use your mobile number for a secure passwordless sign-in.'}</p>{!sent?<><input value={phone} onChange={e=>setPhone(e.target.value)} placeholder="10-digit mobile number" inputMode="tel" style={{width:'100%',height:50,padding:12,border:'1px solid #e9e9ee',margin:'20px 0'}}/><button className="button" onClick={()=>setSent(true)}>Send OTP</button></>:<><input placeholder="6-digit OTP" inputMode="numeric" style={{width:'100%',height:50,padding:12,border:'1px solid #e9e9ee',margin:'20px 0'}}/><button className="button">Verify & continue</button></>}</section></main>}
