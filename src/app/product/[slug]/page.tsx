import type { Metadata } from 'next';
import Link from 'next/link';
import { api } from '@/lib/api';
import ProductActions from '@/components/ProductActions';
import ProductReviews from '@/components/ProductReviews';

type Variant = { id: string | number; sku?: string; label?: string; size?: string; color?: string; colour?: string; stock?: number; inventory?: number; price?: number; selling_price?: number; mrp?: number; image?: string; media?: Array<{ url?: string; image_url?: string }> };
type Product = Record<string, any>;
function unwrap(r: any) { return r?.data?.product ?? r?.data ?? r?.product ?? r ?? null; }
function variantsOf(p: Product): Variant[] { const v = p?.variants ?? p?.variant_options ?? p?.options ?? []; return Array.isArray(v) ? v : []; }
function stringsOf(values: Array<string | undefined>): string[] { return values.filter((value): value is string => Boolean(value)); }
function mediaOf(p: Product, variants: Variant[]) { const direct = Array.isArray(p?.images) ? p.images : []; const media = Array.isArray(p?.media) ? p.media.map((m: any) => m?.url ?? m?.image_url).filter(Boolean) : []; const variantMedia = variants.flatMap(v => Array.isArray(v.media) ? v.media.map(m => m?.url ?? m?.image_url).filter(Boolean) : []).concat(variants.map(v => v.image).filter(Boolean)); return Array.from(new Set([...direct, ...media, p?.image, ...variantMedia].filter(Boolean))); }
function number(...values: any[]) { for (const v of values) if (v !== undefined && v !== null && v !== '') return Number(v); return 0; }
async function getProduct(slug: string) { try { return unwrap(await api<any>(`/storefront/products/${encodeURIComponent(slug)}`)); } catch { return null; } }
export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }): Promise<Metadata> { const { slug } = await params; const p = await getProduct(slug); if (!p) return { title: 'Product unavailable' }; const image = mediaOf(p, variantsOf(p))[0]; const description = p.description || `Shop ${p.name} at PRIYASA.`; return { title: p.name, description, alternates: { canonical: `/product/${encodeURIComponent(p.slug || slug)}` }, openGraph: { title: p.name, description, type: 'website', ...(image ? { images: [{ url: image, alt: p.name }] } : {}) } }; }
export default async function ProductPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params; const p = await getProduct(slug);
  if (!p) return <main className="catalogPage"><div className="emptyState"><span className="eyebrow">PRIYASA / PRODUCT</span><h1>Product unavailable</h1><p>This product could not be loaded from PriyasaCore.</p><Link className="button" href="/shop">Continue shopping</Link></div></main>;
  const variants = variantsOf(p); const images = mediaOf(p, variants); const first = variants[0]; const price = number(p.price, p.pricing?.selling_price, first?.selling_price, first?.price); const mrp = number(p.mrp, p.pricing?.mrp, first?.mrp); const discount = mrp > price ? Math.round((1 - price / mrp) * 100) : 0; const sizes = stringsOf(variants.map(v => v.size || v.label)); const colors = stringsOf(variants.map(v => v.color || v.colour)); const totalStock = variants.length ? variants.reduce((sum, v) => sum + number(v.stock, v.inventory), 0) : number(p.stock, p.inventory, p.available_quantity); const available = p.in_stock !== false && totalStock > 0; const variantId = p.variant_id ?? first?.id ?? '';
  const productUrl = `${process.env.NEXT_PUBLIC_STORE_URL || 'http://localhost:3000'}/product/${encodeURIComponent(p.slug || slug)}`; const jsonLd = { '@context':'https://schema.org', '@type':'Product', name:p.name, url:productUrl, description:p.description || undefined, image:images.length ? images : undefined, category:p.category?.name || p.category || undefined, offers:{ '@type':'Offer', priceCurrency:p.currency || p.pricing?.currency || 'INR', price, availability:available ? 'https://schema.org/InStock' : 'https://schema.org/OutOfStock', url:productUrl }, ...(p.rating ? { aggregateRating:{ '@type':'AggregateRating', ratingValue:p.rating, reviewCount:p.reviewCount || p.review_count || 1 } } : {}) };
  return <><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd) }} /><main className="pdp"><div className="pdpGallery"><div className="pdpMainImage">{images[0] ? <img src={images[0]} alt={p.name} /> : <div className="emptyInline">No product image</div>}</div>{images.length > 1 && <div className="pdpThumbs">{images.slice(0, 8).map((src, i) => <div key={String(src)+i} className="pdpThumb"><img src={src} alt={`${p.name} view ${i + 1}`} /></div>)}</div>}</div><section className="pdpInfo"><span className="eyebrow">PRIYASA / {p.category?.name || p.category || 'COLLECTION'}</span><h1>{p.name}</h1><div className="pdpRating">★ {number(p.rating, p.average_rating) || 4.5} <span>{number(p.reviewCount, p.review_count, p.reviews?.total)} reviews</span></div><div className="pdpPrice">₹{price.toLocaleString('en-IN')} {mrp > price && <><del>₹{mrp.toLocaleString('en-IN')}</del><b>{discount}% OFF</b></>}</div>{available ? <p className="stockState">✓ In stock{totalStock > 0 && totalStock <= 5 ? ` · Only ${totalStock} left` : ''}</p> : <p className="stockState out">Currently out of stock</p>}<div className="pdpLine" /><p className="pdpDescription">{p.description || 'Designed for comfortable everyday styling with the PRIYASA finish.'}</p>{(p.size_chart || p.sizeChart) && <div className="deliveryBox"><strong>Size guide</strong><p>{typeof (p.size_chart || p.sizeChart) === 'string' ? (p.size_chart || p.sizeChart) : 'Size guide available for this product.'}</p></div>}<div className="deliveryBox"><strong>Delivery & returns</strong><p>Enter your pincode at checkout to confirm delivery. Eligible products support easy returns.</p></div><ProductActions variantId={variantId} sizes={sizes} colors={colors} variants={variants} />{!available && <p className="actionMessage" role="status">This item cannot be added until stock is available.</p>}</section></main><main className="catalogPage"><ProductReviews productId={p.id ?? p.product_id ?? slug} /></main></>;
}
