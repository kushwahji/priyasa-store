'use client';

import Link from 'next/link'; import { useParams } from 'next/navigation'; import { useEffect, useState } from 'react'; import AuthGuard from '@/components/AuthGuard'; import { api } from '@/lib/api'; import OrderTimeline from '@/components/OrderTimeline';

type Order={id:string|number;order_number?:string;status?:string;payment_status?:string;total?:number;grand_total?:number;created_at?:string;items?:any[];shipping_address?:any;address?:any;shipment?:any};
function OrderDetailInner(){const params=useParams<{id:string}>();const[order,setOrder]=useState<Order|null>(null);const[loading,setLoading]=useState(true);const[error,setError]=useState('');const[busy,setBusy]=useState('');const[message,setMessage]=useState('');async function load(){if(!params.id)return;setLoading(true);setError('');try{const r=await api<any>(`/storefront/orders/${encodeURIComponent(params.id)}`);setOrder(r.data?.order||r.data||r.order||null)}catch(e){setError(e instanceof Error?e.message:'Unable to load order')}finally{setLoading(false)}}useEffect(()=>{load()},[params.id]);async function cancel(){if(!order||busy||!window.confirm('Cancel this order?'))return;setBusy('cancel');setError('');try{await api(`/storefront/orders/${encodeURIComponent(String(order.id))}/cancel`,{method:'POST',body:JSON.stringify({reason:'Customer requested cancellation'})});setMessage('Order cancelled successfully.');await load()}catch(e){setError(e instanceof Error?e.message:'Cancellation failed')}finally{setBusy('')}}async function requestReturn(){const reason=window.prompt('Reason for return?');if(!reason||busy||!order)return;setBusy('return');setError('');try{await api(`/storefront/orders/${encodeURIComponent(String(order.id))}/returns`,{method:'POST',body:JSON.stringify({reason})});setMessage('Return request submitted.');await load()}catch(e){setError(e instanceof Error?e.message:'Return request failed')}finally{setBusy('')}}async function reorder(){if(!order||busy)return;setBusy('reorder');setError('');try{await api(`/orders/${encodeURIComponent(String(order.id))}/reorder`);setMessage('Items were sent to your bag.');setTimeout(()=>{window.location.href='/cart'},250)}catch(e){setError(e instanceof Error?e.message:'Unable to reorder this purchase')}finally{setBusy('')}}if(loading)return <main className="accountPage"><p className="muted">Loading order…</p></main>;if(error&&!order)return <main className="accountPage"><div className="formError" role="alert">{error}</div><Link className="button" href="/orders">Back to orders</Link></main>;if(!order)return <main className="accountPage"><div className="emptyState"><h1>Order not found</h1><Link className="button" href="/orders">Back to orders</Link></div></main>;const items=order.items||[];const address=order.shipping_address||order.address||{};const status=String(order.status||'processing').toLowerCase();const cancellable=['pending','pending_payment','payment_failed','placed','confirmed','processing'].includes(status);const returnable=['delivered','completed'].includes(status);const shipment=order.shipment||{};return <main className="accountPage"><div className="sectionHead"><div><span className="eyebrow">MY PRIYASA / ORDER</span><h1>Order #{order.order_number||order.id}</h1></div><Link className="textLink" href="/orders">← Orders</Link></div>{message&&<p className="formMessage" role="status">{message}</p>}{error&&<div className="formError" role="alert">{error}</div>}<div className="checkoutGrid"><section><div className="checkoutCard"><div className="orderStatus"><strong>{order.status||'Processing'}</strong><span>{order.payment_status||'Payment pending/processing'}</span></div>{shipment.awb&&<p><b>Tracking:</b> {shipment.courier_name||shipment.courier||'Courier'} · {shipment.awb}</p>}<p className="muted">Placed {order.created_at?new Date(order.created_at).toLocaleString('en-IN'):'recently'}</p><div className="filterChips"><Link className="filterChip" href={`/orders/${order.id}/tracking`}>Track order</Link><Link className="filterChip" href={`/orders/${order.id}/invoice`}>Invoice</Link></div></div><OrderTimeline orderId={order.id}/><div className="checkoutCard"><h2>Items</h2>{items.length?items.map((item,i)=><div className="cartItem" key={item.id||i}><div className="cartThumb">{item.image?<img src={item.image} alt=""/>:<span>PRIYASA</span>}</div><div><b>{item.name||item.product_name||'Product'}</b><p className="muted">Qty {item.quantity||1}{item.size?` · ${item.size}`:''}</p></div><strong>₹{Number(item.total||item.line_total||item.price||0).toLocaleString('en-IN')}</strong></div>):<p className="muted">Item details are not available from the API response.</p>}</div><div className="checkoutCard"><h2>Delivery address</h2><p>{address.recipient_name||address.name||''}<br/>{address.line1||address.address_line1||''}{address.line2||address.address_line2?<><br/>{address.line2||address.address_line2}</>:null}<br/>{address.city||''}, {address.state||''} {address.postal_code||address.pincode||''}</p></div></section><aside className="summary"><span className="eyebrow">ORDER TOTAL</span><div><span>Total</span><strong>₹{Number(order.total||order.grand_total||0).toLocaleString('en-IN')}</strong></div>{cancellable&&<button className="button secondary" disabled={!!busy} onClick={cancel}>{busy==='cancel'?'Cancelling…':'Cancel order'}</button>}{returnable&&<button className="button" disabled={!!busy} onClick={requestReturn}>{busy==='return'?'Submitting…':'Request return'}</button>}{items.length>0&&<button className="button secondary" disabled={!!busy} onClick={reorder}>{busy==='reorder'?'Adding…':'Buy again'}</button>}<Link className="textLink" href="/support">Need help with this order?</Link></aside></div></main>}
export default function OrderDetail(){return <AuthGuard><OrderDetailInner/></AuthGuard>}
