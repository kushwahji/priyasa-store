'use client';
import Link from 'next/link';import {useParams} from 'next/navigation';import {useEffect,useState} from 'react';import {api} from '@/lib/api';
type Order={id:string|number;order_number?:string;status?:string;payment_status?:string;total?:number;grand_total?:number;created_at?:string;items?:any[];shipping_address?:any;address?:any;tracking_number?:string;carrier?:string};
export default function OrderDetail(){const params=useParams<{id:string}>();const [order,setOrder]=useState<Order|null>(null);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const [busy,setBusy]=useState('');const [message,setMessage]=useState('');useEffect(()=>{if(!params.id)return;api<any>(`/orders/${encodeURIComponent(params.id)}`).then(r=>setOrder(r.data?.order||r.data||r.order||null)).catch(e=>setError(e instanceof Error?e.message:'Unable to load order')).finally(()=>setLoading(false))},[params.id]);async function cancel(){if(!order||busy||!window.confirm('Cancel this order?'))return;setBusy('cancel');setError('');try{await api(`/orders/${encodeURIComponent(String(order.id))}/cancel`,{method:'POST',body:JSON.stringify({reason:'Customer requested cancellation'})});setOrder({...order,status:'cancelled'});setMessage('Cancellation request submitted.')}catch(e){setError(e instanceof Error?e.message:'Cancellation failed')}finally{setBusy('')}}async function requestReturn(){const reason=window.prompt('Reason for return?');if(!reason||busy)return;setBusy('return');setError('');try{await api(`/orders/${encodeURIComponent(String(order?.id))}/returns`,{method:'POST',body:JSON.stringify({reason})});setMessage('Return request submitted.')}catch(e){setError(e instanceof Error?e.message:'Return request failed')}finally{setBusy('')}}if(loading)return <main className="accountPage"><p className="muted">Loading order…</p></main>;if(error&&!order)return <main className="accountPage"><div className="formError">{error}</div><Link className="button" href="/orders">Back to orders</Link></main>;if(!order)return <main className="accountPage"><div className="emptyState"><h1>Order not found</h1><Link className="button" href="/orders">Back to orders</Link></div></main>;const items=order.items||[];const address=order.shipping_address||order.address||{};const status=String(order.status||'processing').toLowerCase();const cancellable=['pending','placed','confirmed','processing'].includes(status);const returnable=['delivered','completed'].includes(status);return <main className="accountPage"><div className="sectionHead"><div><span className="eyebrow">MY PRIYASA / ORDER</span><h1>Order #{order.order_number||order.id}</h1></div><Link className="textLink" href="/orders">← Orders</Link></div>{message&&<p className="formMessage">{message}</p>}{error&&<div className="formError">{error}</div>}<div className="checkoutGrid"><section><div className="checkoutCard"><div className="orderStatus"><strong>{order.status||'Processing'}</strong><span>{order.payment_status||'Payment pending/processing'}</span></div>{order.tracking_number&&<p><b>Tracking:</b> {order.carrier||'Courier'} · {order.tracking_number}</p>}<p className="muted">Placed {order.created_at?new Date(order.created_at).toLocaleString('en-IN'):'recently'}</p><div className="heroActions"><Link className="button secondary" href={`/orders/${encodeURIComponent(String(order.id))}/tracking`}>Track order</Link><Link className="textLink" href={`/orders/${encodeURIComponent(String(order.id))}/invoice`}>View invoice →</Link></div></div><div className="checkoutCard"><h2>Items</h2>{items.length?items.map((item,i)=><div className="cartItem" key={item.id||i}><div className="cartThumb">{item.image?<img src={item.image} alt=""/>:null}</div><div><b>{item.name||item.product_name||'Product'}</b><p className="muted">Qty {item.quantity||1}{item.size?` · ${item.size}`:''}</p></div><strong>₹{Number(item.total||item.price||0).toLocaleString('en-IN')}</strong></div>):<p className="muted">Item details are not available from the API response.</p>}</div><div className="checkoutCard"><h2>Delivery address</h2><p>{address.name||''}<br/>{address.address_line1||address.line1||''}{address.address_line2?<><br/>{address.address_line2}</>:null}<br/>{address.city||''}, {address.state||''} {address.postal_code||address.pincode||''}</p></div></section><aside className="summary"><span className="eyebrow">ORDER TOTAL</span><div><span>Total</span><strong>₹{Number(order.total||order.grand_total||0).toLocaleString('en-IN')}</strong></div>{cancellable&&<button className="button secondary" disabled={!!busy} onClick={cancel}>{busy==='cancel'?'Cancelling…':'Cancel order'}</button>}{returnable&&<button className="button" disabled={!!busy} onClick={requestReturn}>{busy==='return'?'Submitting…':'Request return'}</button>}<Link className="textLink" href="/support">Need help with this order?</Link></aside></div></main>}
