'use client';
import { useEffect,useState } from 'react';
import Link from 'next/link';
import { api } from '@/lib/api';
export default function Orders(){const [orders,setOrders]=useState<any[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');useEffect(()=>{api<any>('/storefront/orders').then(r=>setOrders(r.data?.orders||r.data||r.orders||[])).catch(e=>setError(e instanceof Error?e.message:'Unable to load orders.')).finally(()=>setLoading(false))},[]);return <main className="accountPage"><span className="eyebrow">MY PRIYASA</span><h1>Orders</h1>{error&&<div className="formError" role="alert">{error}</div>}{loading?<p className="muted">Loading your orders…</p>:orders.length?<div className="orderList">{orders.map((o,i)=><article className="orderCard" key={o.id||i}><div><strong>Order #{o.order_number||o.id}</strong><small>{o.status||'Processing'}</small></div><div><b>₹{Number(o.total||o.grand_total||0).toLocaleString('en-IN')}</b><Link href={`/orders/${o.id}`}>View order →</Link></div></article>)}</div>:<div className="emptyState"><h2>No orders yet</h2><p>Your completed purchases will appear here.</p><Link className="button" href="/shop">Start shopping</Link></div>}</main>}
