'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {api} from '@/lib/api';
export default function Checkout(){const [addresses,setAddresses]=useState<any[]>([]);const [selected,setSelected]=useState('');const [coupon,setCoupon]=useState('');const [busy,setBusy]=useState(false);const [error,setError]=useState('');useEffect(()=>{api<any>('/storefront/addresses').then(r=>{const a=r.data?.addresses||r.data||r.addresses||[];setAddresses(a);if(a[0])setSelected(String(a[0].id))}).catch(e=>setError(e instanceof Error?e.message:'Unable to load addresses'))},[]);async function placeOrder(){if(!selected){setError('Select a delivery address.');return}setBusy(true);setError('');try{const r=await api<any>('/storefront/checkout/create-order',{method:'POST',body:JSON.stringify({shipping_address_id:selected,coupon_code:coupon.trim()||undefined,payment_method:'razorpay'})});const orderId=r.data?.id||r.data?.order_id||r.order_id;if(!orderId)throw new Error('Checkout did not return an order id.');window.location.href=`/checkout/payment?order=${encodeURIComponent(orderId)}`}catch(e){setError(e instanceof Error?e.message:'Unable to create order')}finally{setBusy(false)}}return <main className="checkoutPage"><div className="sectionHead"><div><span className="eyebrow">PRIYASA / CHECKOUT</span><h1>Secure checkout</h1></div><Link className="textLink" href="/cart">← Bag</Link></div>{error&&<div className="formError">{error}</div>}<div className="checkoutGrid"><section><div className="checkoutCard"><h2>1. Delivery address</h2>{addresses.length?addresses.map(a=><button type="button" key={a.id} className={`addressChoice ${selected===String(a.id)?'selected':''}`} onClick={()=>setSelected(String(a.id))}><b>{a.name||'Delivery address'}</b><span>{a.address_line1||a.line1}, {a.city}, {a.state} {a.postal_code||a.pincode}</span></button>):<div className="emptyInline"><p>No saved address found.</p><Link href="/addresses">Add delivery address</Link></div>}</div><div className="checkoutCard"><h2>2. Offers & coupon</h2><div className="coupon"><input value={coupon} onChange={e=>setCoupon(e.target.value.toUpperCase())} placeholder="Enter coupon code"/><span className="muted">Validated by PriyasaCore during checkout</span></div></div><div className="checkoutCard"><h2>3. Payment</h2><div className="paymentChoice"><b>Razorpay</b><span>UPI, cards, net banking & wallets</span></div></div></section><aside className="summary"><span className="eyebrow">ORDER SUMMARY</span><p className="muted">Final shipping, tax, discount and total are calculated by PriyasaCore.</p><button className="button" disabled={busy||!selected} onClick={placeOrder}>{busy?'Creating order…':'Continue to payment'}</button></aside></div></main>}
