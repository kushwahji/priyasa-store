'use client';import {useEffect,useState} from 'react';import Link from 'next/link';import AuthGuard from '@/components/AuthGuard';import {api} from '@/lib/api';
const empty={label:'Home',recipient_name:'',phone:'',line1:'',line2:'',city:'',state:'',postal_code:'',country:'IN',is_default:false};
function AddressesContent(){const [items,setItems]=useState<any[]>([]);const [form,setForm]=useState<any>(empty);const [open,setOpen]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');async function load(){try{const r=await api<any>('/storefront/addresses');setItems(r.data?.addresses||r.addresses||r.data||[])}catch(e){setError(e instanceof Error?e.message:'Unable to load addresses')}}useEffect(()=>{load()},[]);function change(k:string,v:string|boolean){setForm((x:any)=>({...x,[k]:v}))}async function save(e:React.FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await api<any>('/storefront/addresses',{method:'POST',body:JSON.stringify(form)});const a=r.data?.address||r.address||r.data;if(a)setItems(x=>[...x,a]);else await load();setForm(empty);setOpen(false)}catch(e){setError(e instanceof Error?e.message:'Unable to save address')}finally{setBusy(false)}}return <main className="accountPage"><div className="sectionHead"><div><span className="eyebrow">PRIYASA / ACCOUNT</span><h1>Delivery addresses</h1></div><Link className="textLink" href="/checkout">Back to checkout →</Link></div>{error&&<div className="formError" role="alert">{error}</div>}<button className="button" onClick={()=>setOpen(true)}>+ Add new address</button>{open&&<form className="addressForm" onSubmit={save}><h2>Add delivery address</h2>{(['recipient_name','phone','line1','line2','city','state','postal_code'] as const).map(k=><label key={k}>{k==='recipient_name'?'Recipient name':k==='line1'?'Address line 1':k==='line2'?'Address line 2':k==='postal_code'?'PIN code':k[0].toUpperCase()+k.slice(1)}<input required={k!=='line2'} value={form[k]} onChange={e=>change(k,e.target.value)} inputMode={k==='phone'||k==='postal_code'?'numeric':undefined}/></label>)}<label>Set as default<input type="checkbox" checked={form.is_default} onChange={e=>change('is_default',e.target.checked)}/></label><div><button className="button" disabled={busy}>{busy?'Saving…':'Save address'}</button><button type="button" className="textButton" onClick={()=>setOpen(false)}>Cancel</button></div></form>}<div className="addressList">{items.map(a=><article className="checkoutCard" key={a.id}><strong>{a.recipient_name||a.name}</strong>{a.label&&<small>{a.label}</small>}<p>{a.line1||a.address_line1}{a.line2&&`, ${a.line2}`}</p><p>{a.city}, {a.state} {a.postal_code||a.pincode}</p>{a.phone&&<small>{a.phone}</small>}</article>)}</div>{!items.length&&!open&&<div className="emptyState"><h2>No saved addresses</h2><p>Add an address to make checkout faster.</p></div>}</main>}
export default function Addresses(){return <AuthGuard><AddressesContent/></AuthGuard>}
